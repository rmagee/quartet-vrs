============
Installation
============

At the command line::

    $ easy_install quartet_vrs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv quartet_vrs
    $ pip install quartet_vrs
